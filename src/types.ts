export type TMultiplication = {
  multiplying: number;
  multiplier: number;
  result: number;
};
export type TDivision = {
  dividend: number;
  divider: number;
};
export type TCircleArea = {
  radius: number;
};
